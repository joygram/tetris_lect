---
layout: default
title: 7단계 - 점수 및 레벨 시스템
---

# 7단계: 점수 및 레벨 시스템

## 🎯 이 단계에서 배울 것

- 점수 계산 로직
- 레벨 시스템 구현
- UI 연동
- 낙하 속도 조정

## 📝 실습 내용

### 1. GameController 클래스 생성

`Assets/Scripts/GameController.cs` 생성:

```csharp
using UnityEngine;
using UnityEngine.UI;

public class GameController : MonoBehaviour
{
    public int score = 0;
    public int lines = 0;
    public int level = 1;
    
    public Text scoreText;
    public Text linesText;
    public Text levelText;
    
    void Start()
    {
        UpdateUI();
    }
    
    public void AddScore(int linesCleared)
    {
        int points = 0;
        switch (linesCleared)
        {
            case 1: points = 100; break;
            case 2: points = 300; break;
            case 3: points = 500; break;
            case 4: points = 800; break;
        }
        
        score += points * level;
        lines += linesCleared;
        
        // 10줄마다 레벨 증가
        level = (lines / 10) + 1;
        
        UpdateUI();
    }
    
    void UpdateUI()
    {
        if (scoreText) scoreText.text = $"점수: {score}";
        if (linesText) linesText.text = $"줄: {lines}";
        if (levelText) levelText.text = $"레벨: {level}";
    }
}
```

## ✅ 확인 사항

- [ ] 점수가 올바르게 계산되는가?
- [ ] 레벨이 증가하는가?
- [ ] UI에 점수가 표시되는가?

## 📚 참고 자료

- [스코어 UI 표시 가이드](../참고자료/스코어-UI-표시-가이드)
- [UI 배치 구조 설계](../참고자료/UI배치구조설계)

---

[← 이전 단계: 6단계 - 행 제거 시스템](./6단계-행-제거-시스템) | [다음 단계: 8단계 - 게임 완성 →](./8단계-게임-완성)
